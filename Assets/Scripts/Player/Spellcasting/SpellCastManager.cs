using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpellCastManager : MonoBehaviour
{
    internal void StartCast()
    {
        // if already animating Cast interupted 
        // if already animating Cast interupted 
    }

    internal bool TryFireSpell()
    {
        // TODO 
        // Check if the spell is valid and Cast Key is called at the right time.
        return false;
    }

    internal void TryTransmuteSpell()
    {
        // TODO 
        // if already animating Cast interupted 
        // Check if the spell (spell sequence) exists and transmute it.
    }

    /**
     * TODO for the future  
     */
    internal void TryConjureSpell()
    {
        // TODO 
        // if already animating Cast interupted 
        // Check if the spell (spell sequence) exists and transmute it.
    }


    /**
     * TODO for the future 
     */
    internal void TryCharmSpell()
    {
        // TODO 
        // if already animating Cast interupted 
        // Check if the spell (spell sequence) exists and transmute it.
    }
}
